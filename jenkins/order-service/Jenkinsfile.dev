pipeline {
    agent any

    tools {
        maven 'Maven3'
    }

    environment {
        SERVICE_NAME = 'order-service'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Debug Maven') {
            steps {
                sh 'which mvn || echo "Maven not found"'
                sh 'mvn --version || echo "Maven command failed"'
            }
        }

        stage('Test') {
            steps {
                dir("${SERVICE_NAME}") {
                    sh 'mvn clean test'
                }
            }
        }
    }
}